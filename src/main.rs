// The active binary for this project is configured in Cargo.toml:
//
// [[bin]]
// name = "base_app"
// path = "src/engine.rs"
//
// Keep server startup code in src/engine.rs so `cargo run` uses one source of truth.
